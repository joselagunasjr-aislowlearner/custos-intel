{
  "name": "custos-intel",
  "version": "0.1.0",
  "private": true,
  "description": "Fire & Life Safety Plan Review Application",
  "workspaces": [
    "packages/api",
    "packages/web"
  ],
  "scripts": {
    "api:dev": "npm run start:dev --workspace=packages/api",
    "api:build": "npm run build --workspace=packages/api",
    "web:dev": "npm run dev --workspace=packages/web",
    "web:build": "npm run build --workspace=packages/web"
  }
}
